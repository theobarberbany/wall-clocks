
---
apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  creationTimestamp: null
  name: wallclock.wallclocks.ziglu.io
spec:
  group: wallclocks.ziglu.io
  names:
    kind: WallClock
    plural: wallclock
    shortNames:
    - wclock
    - wc
    - wcs
  scope: Cluster
  validation:
    openAPIV3Schema:
      description: WallClock is the Schema for the wallclocks API
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/api-conventions.md#resources'
          type: string
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/api-conventions.md#types-kinds'
          type: string
        metadata:
          type: object
        spec:
          description: WallClockSpec defines the desired state of WallClock
          properties:
            timezone:
              description: Timezone is the timezone for the clock
              type: string
          type: object
        status:
          description: WallClockStatus defines the observed state of a WallClock
          properties:
            conditions:
              description: Conditions gives detailed condition information about the
                clock
              items:
                description: WallClockCondition is a status condition for a WallClock
                properties:
                  lastTransitionTime:
                    description: LastTransitionTime of this condition
                    format: date-time
                    type: string
                  lastUpdateTime:
                    description: LastUpdateTime of this condition
                    format: date-time
                    type: string
                  message:
                    description: Message associated with this condition
                    type: string
                  reason:
                    description: Reason for the current status of this condition
                    type: string
                  status:
                    description: Status of this condition
                    type: string
                  type:
                    description: Type of this condition
                    type: string
                required:
                - status
                - type
                type: object
              type: array
            phase:
              description: Phase is used to determine which phase of the clock cycle
                a clock is currently in.
              type: string
            time:
              description: Time is the time on the WallClock
              format: date-time
              type: string
          required:
          - phase
          - time
          type: object
      type: object
  versions:
  - name: v1
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
